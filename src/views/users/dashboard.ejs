<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CryptoApp dashboard</title>
</head>
<body>
    <form method="post" action="/users/symbols/add">
        <input type="text" name="symbol" placeholder="Crypto token">
        <button>Add Symbol</button>
    </form>

    <br/>
    <br/>

    <table>
        <thead>
            <tr>
                <th>Symbol</th>
                <th>Value</th>
            </tr>
        </thead>
        <tbody>
            <% userSymbols.forEach(userSymbol => { %>
                <tr>
                    <td><%= userSymbol.symbol %></td>
                    <td><%= symbolValues.find(e => e?.symbol === userSymbol?.symbol)?.value %></td>
                </tr>
            <% }) %>
        </tbody>
    </table>
</body>
</html>